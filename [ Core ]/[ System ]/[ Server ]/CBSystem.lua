-- --------------------------------------------------------------------------------
-- ---------------------------------- DokusCore -----------------------------------
-- --------------------------------------------------------------------------------
_G.TSTSC = function(Event)
  local Result = nil
  local P = promise.new()
  TriggerEvent(Event, function(R) Result = R end)
  P.resolve(P)
  Citizen.Await(P)
  return Result
end
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
