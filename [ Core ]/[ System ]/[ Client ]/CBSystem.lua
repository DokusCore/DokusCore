--------------------------------------------------------------------------------
---------------------------------- DokusCore -----------------------------------
--------------------------------------------------------------------------------
_G.TCTCC = function(Event)
  local Result = nil
  local P = promise.new()
  TriggerEvent(Event,  function(R) Result = R end)
  P.resolve(Result)
  local T = Citizen.Await(Result)
  return Result
end
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
